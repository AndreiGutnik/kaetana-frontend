import { ContainerContacts, Title } from './Contacts.styled';

const Contacts = () => {
  return (
    <ContainerContacts>
      <Title>Контакти</Title>
    </ContainerContacts>
  );
};

export default Contacts;
